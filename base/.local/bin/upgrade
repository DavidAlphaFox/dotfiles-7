#!/bin/zsh

# update dotfiles
echo -e "\e[1;34m Updating dotfiles..."
cd ~/.dotfiles
git stash -q
git pull 
git stash pop
~/.dotfiles/base/.local/bin/restow

# upgrade all packages
echo -e "\n\e[1;34m Updating system..."
yay -Syu

# removed orphaned packages
orphans="$(yay -Qdtq)"
if [ -n "$orphans" ]; then
  echo -e "\n\e[1;34m Removing orphaned packages..."
  yay -Rns $(echo $orphans)
fi

# removed cached packages
if [ -n "$(find /tmp/pacman/pkg -type f)" ]; then
  echo -e "\n\e[1;34m Removing cached packages..."
  yes | sudo yay -Scc 2>&1 >/dev/null
fi

# dedup btrfs
if [ -n "$dodedup" ]; then
  echo -e "\n\e[1;34m Deduplicating filesystem..."
  device="$(findmnt -nvo SOURCE /)"
  mntpnt="$(mktemp -d --tmpdir duperemove-XXXXXX)"
  mkdir -p $mntpnt
  sudo mount $device $mntpnt
  pushd -q $mntpnt
  sudo duperemove -rdhv .
  popd -q
  sudo umount $mntpnt
  rmdir $mntpnt
fi

