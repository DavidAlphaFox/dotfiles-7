#!/bin/zsh

echo
for i in $(find . -iname '.git' -type d -printf '%P\0' 2>/dev/null | xargs -0 dirname | sort); do
  echo -e "\e[1;33m $i (git $@)\e[0m"
  pushd -q $i
  git "$@"
  popd -q
  echo
done

