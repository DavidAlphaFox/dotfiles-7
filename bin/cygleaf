#!/bin/bash

CYGPIPE="/tmp/cygleaf"
CYGDEPS="$HOME/.dotfiles/.base"
if [ ! -f "$CYGDEPS" ]; then
    touch $CYGDEPS
fi
mkfifo $CYGPIPE
echo "checking dependencies, please wait" >&2
cygcheck-dep -pl | tee $CYGPIPE | cat - $CYGDEPS | sort | uniq -u | cat $CYGPIPE - | sort | uniq -d
rm $CYGPIPE
